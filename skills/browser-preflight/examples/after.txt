$ agent: verify the deployed landing page renders after the deploy

> running browser-preflight
> probe 1/3: browser extension channel -> tabs_context ... dead (not connected)
> probe 2/3: computer-use channel -> screenshot ... live

channel = computer-use · fallback = headless browser MCP · if all dead = URL + manual steps

> navigating to https://example-app.dev
> screenshot captured: hero renders, nav renders, no console errors

agent: Deployed landing page verified via computer-use — hero and nav
render correctly, no console errors.

---

$ agent: verify the deployed landing page renders after the deploy
  (all channels dead this time)

> running browser-preflight
> probe 1/3: browser extension channel -> tabs_context ... dead (not connected)
> probe 2/3: computer-use channel -> screenshot ... dead (locked by another session)
> probe 3/3: headless browser MCP -> ping ... dead (not installed)

channel = none · fallback = none · if all dead = degraded deliverable

agent: degraded deliverable — no live browser channel this session.
Please verify manually:
  1. Open https://example-app.dev in your own browser
  2. Confirm the hero section and nav render
  3. Open devtools -> Console, confirm no red errors
  4. Reload once to confirm it's not a cache artifact

(Page source fetched and checked separately: title tag and hero markup
are present in the raw HTML, so the deploy at least shipped content.)
